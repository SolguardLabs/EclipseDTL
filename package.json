{
  "name": "eclipsedtl",
  "version": "0.1.0",
  "private": true,
  "description": "Scenario tests for the EclipseDTL liquidity auction engine",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/node/*.test.js",
    "test:rust": "cargo test --locked",
    "ci": "bash scripts/ci.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
